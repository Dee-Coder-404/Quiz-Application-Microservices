<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Register</title>
</head>
<body>
    <h2>Register</h2>
    
    <form th:action="@{/registersuccess}" th:object="${employees}" method="post">
		
        <label for="ename">Name:</label>
        <input type="text" th:field="*{ename}" required><br><br>

        <label for="eid">Eid:</label>
        <input type="number" th:field="*{eid}" required><br><br>
		
		<label for="salary">Salary:</label>
		<input type="number" th:field="*{salary}" required><br><br>
.
        <button type="submit">Register</button>
    </form>
	
	<p th:if="${message}" th:text="${message}"></p>
	
	<h1>------------------------------------------------------------------------------------</h1>
	
	<form th:action="@{/showdetails}" th:object="${employees}" method="get">
		
		<button type="submit">Show Details</button>
		
	</form>
	<h1>-----------------------------------------------------------------------------------</h1>
	<form th:action="@{/update}" th:object="${employees}" method="post">
			
	        <label for="ename">Name:</label>
	        <input type="text" th:field="*{ename}" required><br><br>

	        <label for="eid">Eid:</label>
	        <input type="number" th:field="*{eid}" required><br><br>
			
			<label for="salary">Salary:</label>
			<input type="number" th:field="*{salary}" required><br><br>

	        <button type="submit">Update</button>
			
	    </form>

</body>
</html>
